import React from 'react';
import { LoaderStyle } from './style';

const Loader = () => {
    return (
        <LoaderStyle>
            <span className="loader"></span>
        </LoaderStyle>
    );
};

export default Loader;